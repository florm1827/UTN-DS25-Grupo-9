
{
  "name": "backend-tenis",
  "version": "1.0.0",
  "description": "API REST para reservas de canchas",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "build": "echo 'no build needed for JS'",
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^6.0.0",
    "@prisma/client": "^6.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
